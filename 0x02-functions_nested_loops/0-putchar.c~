#include<main.h>
/**
 *
 *main-Entry Point
 * 
 *Return: Always 0 (Success)
 */
int main(void)
{
 char li[8] = "_putchar"
   int i;

 for(i=0; i <= 7; i++)
   {
     _putchar(li[i]);
   }
 _putchar('\n');
  return (0);
 
}
