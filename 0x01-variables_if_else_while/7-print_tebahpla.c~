#include<stdio.h>
/**
 *main - Entry point
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
char beta;

for (beta = 'a'; beta <= 'z' ; beta++)
{
putchar(beta);
}
for (beta = 'A'; beta <= 'Z'; beta++)
{putchar (beta); }
putchar('\n');
return (0);
}
